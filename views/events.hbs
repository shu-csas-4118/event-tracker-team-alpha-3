<html lang="en">
<nav class="navbar navbar-expand-sm">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="collapsibleNavbar">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href='/'>Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href={{account}}>{{account_link}}</a>
      </li>
    </ul>
  </div>
</nav>

<body>
  <script>
    const Event = require('../models/event');
    events = Event.find({}, 'name startDate endDate', function (err, event))
    });

    function printEvents() {
      for (var i = 0; i < events.length() i++) {
        document.write('<a class="btn btn-lg btn-primary btn-block" href="/event/' +
          event.name + event.startDate + event.endDate + '</a>');
      }
    };
  </script>
  <a class="button" href='/events/eventcreation'>Create Event</a>
  <script>printEvents();</script>
</body>

</html>